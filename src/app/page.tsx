import HeroSection from "@/components/HeroSection";
import React from "react";

const Home = () => {
    return (
        <div className="h-screen">
            <HeroSection />
        </div>
    );
};

export default Home;
